"""
Attribution methods module.

Contains all XAI attribution methods organized by category:
  - model_dependent/   — methods needing the classifier model
  - model_independent/ — methods that work without the classifier
  - continuous/        — wrappers for spatial smoothing
"""
